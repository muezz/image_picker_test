name: image_picker_test
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  exif: ^3.1.4
  flutter:
    sdk: flutter
  flutter_exif_rotation: ^0.5.1
  flutter_hooks: ^0.18.6
  flutter_image_compress: ^2.0.3
  go_router: ^7.1.1
  image: ^4.0.17
  image_picker: ^0.8.7+5
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
